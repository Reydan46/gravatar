# --- Общие настройки ---

# Установите желаемую версию Python (по умолчанию 3.13)
PYTHON_VERSION=3.13

# Временная зона для приложения
TZ=Europe/Minsk

# Включить подробные отладочные логи для log_service.py (True/False) (по умолчанию False)
# SHOW_DEBUG_LOGS=True


# --- Сетевые настройки ---

# Хост для сервера приложения (по умолчанию 0.0.0.0)
# APP_HOST=0.0.0.0
# Порт для сервера приложения (по умолчанию 9999)
APP_PORT=9999
# Внешний порт для Nginx (по умолчанию 9888)
NGINX_PORT=9888


# --- Пути и база данных ---

# Путь к каталогу данных на хост-машине
DATA_PATH=/docker/data/gravatar/
# Внутренний путь к каталогу данных внутри контейнера (по умолчанию /data/)
# INTERNAL_DATA_PATH=/data/

# JWT настройки
# JWT_SECRET_KEY - опционально: если закомментирован, генерируется автоматически в файл jwt_secret_key
JWT_SECRET_KEY=your_secret_key_here
# JWT_ALGORITHM - опционально: если не указан, используется HS256
# JWT_ALGORITHM=HS256


# --- Настройки производительности ---

# Количество воркеров uvicorn. Формула: (количество ядер CPU * количество потоков на ядро) + 1 (по умолчанию 1)
APP_WORKERS=1
# Включить или отключить автоперезагрузку при изменении кода (True/False) (по умолчанию False)
# APP_RELOAD=False

# Настройки безопасности

# CORS_ALLOW_ORIGINS - Управляет, с каких доменов браузеры могут делать запросы к API.
# - Закомментируйте/удалите строку или установите "*" для разрешения всех доменов (поведение по умолчанию).
# - CORS_ALLOW_ORIGINS=https://app.example.com,https://test.example.com - для разрешения конкретных доменов.
# - CORS_ALLOW_ORIGINS= - установите пустое значение для блокировки всех CORS-запросов.
CORS_ALLOW_ORIGINS=*

# ALLOWED_HOSTS - Проверяет заголовок "Host" для защиты от DNS-rebinding атак.
# - Закомментируйте/удалите строку или установите пустое значение для отключения проверки (поведение по умолчанию).
# - ALLOWED_HOSTS=app.example.com,localhost,127.0.0.1 - для разрешения только этих хостов.
#ALLOWED_HOSTS=

# TRUSTED_PROXY_IPS - Управляет доверием к заголовкам от прокси (X-Forwarded-*).
# - Закомментируйте/удалите строку или установите "*" для доверия всем прокси (поведение по умолчанию).
# - TRUSTED_PROXY_IPS=10.1.1.1,10.1.1.2 - для доверия только конкретным IP.
# - TRUSTED_PROXY_IPS= - установите пустое значение для максимальной безопасности (не доверять никому).
TRUSTED_PROXY_IPS=*