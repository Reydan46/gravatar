# ==============================================================================
# ФАЙЛ ЗНАЧЕНИЙ ПО УМОЛЧАНИЮ
# ------------------------------------------------------------------------------
# Этот файл содержит базовые настройки. Переменные из него будут использованы,
# только если они НЕ определены в файле .env.
# Это позволяет .env содержать только те значения, которые вы хотите переопределить.
# ==============================================================================

# --- Общие настройки ---

# Версия Python, используемая в Docker-образе для сборки и запуска приложения (по умолчанию 3.13)
# PYTHON_VERSION=3.13

# Временная зона для контейнера и приложения (например, UTC) (по умолчанию Europe/Minsk)
# TZ=Europe/Minsk

# Включает или отключает подробные отладочные логи для log_service.py (True/False) (по умолчанию False)
# SHOW_DEBUG_LOGS=False

# --- Сетевые настройки ---

# Хост, на котором Uvicorn слушает внутри контейнера (по умолчанию 0.0.0.0)
# "0.0.0.0" позволяет принимать подключения с любого сетевого интерфейса внутри Docker-сети.
# APP_HOST=0.0.0.0

# Внутренний порт для Uvicorn-сервера приложения. Nginx, работающий в том же контейнере,
# будет проксировать запросы на этот порт (по умолчанию 8888)
# APP_PORT=8888

# Внешний порт, который будет открыт на хост-машине и будет обслуживаться Nginx.
# Именно по этому порту сервис будет доступен извне (по умолчанию 9999)
# NGINX_PORT=9999

# --- Пути ---
# Внешний путь к каталогу данных на хост-машине (требуется указать)
DATA_PATH=/docker/data/gravatar/
# Внутренний путь к каталогу данных внутри контейнера (по умолчанию /data/)
# INTERNAL_DATA_PATH=/data/

# --- JWT настройки ---

# Секретный ключ для подписи JWT-токенов.
# Если закомментирован, ключ будет сгенерирован автоматически и сохранен
# в файле jwt_secret_key в каталоге данных (DATA_PATH).
# JWT_SECRET_KEY=

# Алгоритм шифрования для JWT-токенов (по умолчанию HS256)
# JWT_ALGORITHM=HS256

# --- Настройки производительности ---

# Количество рабочих процессов (воркеров) Uvicorn.
# Для продакшена рекомендуется: (кол-во ядер CPU * кол-во потоков на ядро) + 1 (по умолчанию 1)
# APP_WORKERS=1

# Включает или отключает автоматическую перезагрузку сервера при изменении кода.
# Установите "True" только для разработки, в продакшене должно быть "False".
# APP_RELOAD=False

# --- Настройки безопасности ---

# CORS_ALLOW_ORIGINS - Управляет, с каких доменов браузеры могут делать запросы к API.
# - Закомментируйте/удалите строку или установите "*" для разрешения всех доменов (поведение по умолчанию).
# - CORS_ALLOW_ORIGINS=https://app.example.com,https://test.example.com - для разрешения конкретных доменов.
# - CORS_ALLOW_ORIGINS= - установите пустое значение для блокировки всех CORS-запросов.
# CORS_ALLOW_ORIGINS=*

# ALLOWED_HOSTS - Проверяет заголовок "Host" для защиты от атак DNS-перепривязки (DNS rebinding).
# - Закомментируйте/удалите строку или установите "*" для разрешения всех хостов (поведение по умолчанию).
# - ALLOWED_HOSTS=app.example.com,localhost,127.0.0.1 - для разрешения только этих хостов.
# - ALLOWED_HOSTS= - установите пустое значение для блокировки всех хостов.
# ALLOWED_HOSTS=*

# TRUSTED_PROXY_IPS - Управляет доверием к заголовкам от прокси (X-Forwarded-*).
# - Закомментируйте/удалите строку или установите "*" для доверия всем прокси (поведение по умолчанию).
# - TRUSTED_PROXY_IPS=10.1.1.1,10.1.1.2 - для доверия только конкретным IP.
# - TRUSTED_PROXY_IPS= - установите пустое значение для максимальной безопасности (не доверять никому).
# TRUSTED_PROXY_IPS=*

# PROXY_MIDDLEWARE_IGNORE_IPS - Список IP, которые будут проигнорированы Proxy Middleware.
# Запросы от этих IP пройдут "насквозь" без обработки прокси-заголовков.
# Полезно для систем мониторинга или health-check'ов.
# - Закомментируйте/удалите или оставьте пустым, чтобы никто не игнорировался (поведение по умолчанию).
# - PROXY_MIDDLEWARE_IGNORE_IPS=127.0.0.1,192.168.1.100 - для пропуска обработки этих IP.
# PROXY_MIDDLEWARE_IGNORE_IPS=

# TRUST_DOCKER_GATEWAY - Автоматически доверяет шлюзу Docker как прокси.
# - Установите "True" для добавления IP шлюза Docker в TRUSTED_PROXY_IPS (поведение по умолчанию).
# - Установите "False", чтобы отключить это поведение и управлять TRUSTED_PROXY_IPS полностью вручную.
# TRUST_DOCKER_GATEWAY=True